filename: ./.env
content: |
  # Server configuration
  SERVER_HOST=127.0.0.1
  SERVER_PORT=8000

  # Email configuration
  EMAIL_ADDRESS=your_email@gmail.com
  OAUTH2_FILE_PATH=path/to/oauth2_credentials.json

  # Electron configuration
  APP_URL=http://${SERVER_HOST}:${SERVER_PORT}
